Koodkan magiliyaa snack expo 

import React, { useState } from 'react';
import {
  StyleSheet,
  Text,
  View,
  Image,
  TouchableOpacity,
  ScrollView,
  SafeAreaView,
} from 'react-native';
import { Ionicons, MaterialIcons } from '@expo/vector-icons';

export default function UserProfileScreen() {
  const [isFollowing, setIsFollowing] = useState(false);
  const [isBlocked, setIsBlocked] = useState(false);

  return (
    <SafeAreaView style={styles.container}>
      <ScrollView showsVerticalScrollIndicator={false}>
        
        {/* Cover Photo */}
        <View style={styles.coverContainer}>
          <Image
            source={{ uri: 'https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?w=800' }}
            style={styles.coverImage}
          />
          {/* Top Actions */}
          <View style={styles.topActions}>
            <TouchableOpacity style={styles.iconButton}>
              <Ionicons name="arrow-back" size={24} color="#fff" />
            </TouchableOpacity>
            <TouchableOpacity style={styles.iconButton}>
              <MaterialIcons name="more-vert" size={24} color="#fff" />
            </TouchableOpacity>
          </View>
        </View>

        {/* Profile Info Section */}
        <View style={styles.profileSection}>
          {/* Avatar & Badges */}
          <View style={styles.avatarRow}>
            <Image
              source={{ uri: 'https://images.unsplash.com/photo-1535713875002-d1d0cf377fde?w=200' }}
              style={styles.avatar}
            />
            <View style={styles.statsContainer}>
              <View style={styles.statBox}>
                <Text style={styles.statNumber}>1.4K</Text>
                <Text style={styles.statLabel}>Posts</Text>
              </View>
              <View style={styles.statBox}>
                <Text style={styles.statNumber}>25.8K</Text>
                <Text style={styles.statLabel}>Followers</Text>
              </View>
              <View style={styles.statBox}>
                <Text style={styles.statNumber}>480</Text>
                <Text style={styles.statLabel}>Following</Text>
              </View>
            </View>
          </View>

          {/* Name & Username */}
          <View style={styles.nameRow}>
            <Text style={styles.name}>Ducaale Daacad</Text>
            {/* Verified Badge */}
            <MaterialIcons name="verified" size={20} color="#6C3BFF" style={styles.verifiedIcon} />
          </View>
          <Text style={styles.username}>@ducaale_official</Text>

          {/* Bio */}
          <Text style={styles.bio}>
            Mobile App Developer 🚀 | Building the future of tech with React Native & Expo ⚡ | DUCAALE DAACAD
          </Text>

          {/* Location */}
          <View style={styles.locationRow}>
            <Ionicons name="location-outline" size={16} color="#8E8E93" />
            <Text style={styles.locationText}>Mogadishu, Somalia</Text>
          </View>

          {/* Action Buttons (Follow / Message / Report / Block) */}
          <View style={styles.actionButtonsRow}>
            <TouchableOpacity
              style={[styles.primaryButton, isFollowing && styles.followingButton]}
              onPress={() => setIsFollowing(!isFollowing)}
            >
              <Text style={styles.primaryButtonText}>
                {isFollowing ? 'Following' : 'Follow'}
              </Text>
            </TouchableOpacity>

            <TouchableOpacity style={styles.secondaryButton}>
              <Text style={styles.secondaryButtonText}>Message</Text>
            </TouchableOpacity>

            <TouchableOpacity 
              style={styles.iconActionButton}
              onPress={() => setIsBlocked(!isBlocked)}
            >
              <Ionicons name={isBlocked ? "ban" : "shield-outline"} size={20} color="#FF3B30" />
            </TouchableOpacity>
          </View>

        </View>

        {/* User Content Tabs Preview */}
        <View style={styles.tabContainer}>
          <TouchableOpacity style={[styles.tab, styles.activeTab]}>
            <Ionicons name="grid-outline" size={20} color="#6C3BFF" />
            <Text style={[styles.tabText, styles.activeTabText]}>Posts</Text>
          </TouchableOpacity>
          <TouchableOpacity style={styles.tab}>
            <Ionicons name="bookmark-outline" size={20} color="#8E8E93" />
            <Text style={styles.tabText}>Saved</Text>
          </TouchableOpacity>
        </View>

      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#0F0E17',
  },
  coverContainer: {
    height: 180,
    width: '100%',
    position: 'relative',
  },
  coverImage: {
    width: '100%H',
    height: '100%',
  },
  topActions: {
    position: 'absolute',
    top: 15,
    left: 15,
    right: 15,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  iconButton: {
    backgroundColor: 'rgba(0,0,0,0.5)',
    padding: 8,
    borderRadius: 20,
  },
  profileSection: {
    paddingHorizontal: 20,
    marginTop: -40,
  },
  avatarRow: {
    flexDirection: 'row',
    alignItems: 'flex-end',
    justifyContent: 'space-between',
  },
  avatar: {
    width: 84,
    height: 84,
    borderRadius: 42,
    borderWidth: 4,
    borderColor: '#0F0E17',
  },
  statsContainer: {
    flexDirection: 'row',
    flex: 1,
    justifyContent: 'space-around',
    marginLeft: 15,
    marginBottom: 5,
  },
  statBox: {
    alignItems: 'center',
  },
  statNumber: {
    color: '#FFFFFE',
    fontSize: 18,
    fontWeight: 'bold',
  },
  statLabel: {
    color: '#8E8E93',
    fontSize: 12,
  },
  nameRow: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 12,
  },
  name: {
    color: '#FFFFFE',
    fontSize: 20,
    fontWeight: 'bold',
  },
  verifiedIcon: {
    marginLeft: 6,
  },
  username: {
    color: '#8E8E93',
    fontSize: 14,
    marginTop: 2,
  },
  bio: {
    color: '#FFFFFE',
    fontSize: 14,
    marginTop: 10,
    lineHeight: 20,
  },
  locationRow: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 8,
  },
  locationText: {
    color: '#8E8E93',
    fontSize: 13,
    marginLeft: 4,
  },
  actionButtonsRow: {
    flexDirection: 'row',
    marginTop: 15,
    gap: 10,
  },
  primaryButton: {
    flex: 2,
    backgroundColor: '#6C3BFF',
    paddingVertical: 10,
    borderRadius: 10,
    alignItems: 'center',
  },
  followingButton: {
    backgroundColor: 'transparent',
    borderWidth: 1,
    borderColor: '#6C3BFF',
  },
  primaryButtonText: {
    color: '#FFFFFE',
    fontWeight: '600',
    fontSize: 14,
  },
  secondaryButton: {
    flex: 2,
    backgroundColor: '#1F1E2E',
    paddingVertical: 10,
    borderRadius: 10,
    alignItems: 'center',
  },
  secondaryButtonText: {
    color: '#FFFFFE',
    fontWeight: '600',
    fontSize: 14,
  },
  iconActionButton: {
    backgroundColor: '#1F1E2E',
    padding: 10,
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },
  tabContainer: {
    flexDirection: 'row',
    borderTopWidth: 1,
    borderTopColor: '#1F1E2E',
    marginTop: 20,
  },
  tab: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    paddingVertical: 12,
    gap: 8,
  },
  activeTab: {
    borderBottomWidth: 2,
    borderBottomColor: '#6C3BFF',
  },
  tabText: {
    color: '#8E8E93',
    fontWeight: '500',
  },
  activeTabText: {
    color: '#FFFFFE',
  },
});
